<UserControl x:Class="ProxiFyre.ApplicationRulesTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#667085" />
        <SolidColorBrush x:Key="BorderBrush" Color="#D8DEE8" />

        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="9,5" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="0" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="应用规则" FontSize="15" FontWeight="SemiBold" />
            <TextBlock Grid.Column="1"
                       Text="{Binding ElementName=AppsList, Path=Items.Count, StringFormat={}{0} 项}"
                       Foreground="{StaticResource MutedTextBrush}"
                       FontSize="12"
                       VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="搜索"
                       Foreground="{StaticResource MutedTextBrush}"
                       VerticalAlignment="Center" />
            <TextBox x:Name="SearchBox"
                     Grid.Column="2"
                     TextChanged="SearchBox_TextChanged"
                     ToolTip="忽略 -、_、空格；按顺序匹配字符" />
        </Grid>

        <ListBox x:Name="AppsList" Grid.Row="2">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="8,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="28" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Width="24"
                                    Height="24"
                                    CornerRadius="4"
                                    Background="#EEF2F7"
                                    VerticalAlignment="Center">
                                <Grid>
                                    <Image Source="{Binding Icon}"
                                           Width="20"
                                           Height="20"
                                           Stretch="Uniform" />
                                    <TextBlock Text="{Binding IconText}"
                                               FontSize="15"
                                               FontWeight="SemiBold"
                                               Foreground="#667085"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </Border>

                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}"
                                               FontWeight="SemiBold"
                                               Foreground="#1F2937" />
                                    <Border Background="#EEF2F7"
                                            CornerRadius="4"
                                            Padding="5,2"
                                            VerticalAlignment="Center"
                                            Margin="8,0,0,0">
                                        <TextBlock Text="{Binding KindText}"
                                                   FontSize="10"
                                                   Foreground="#475467" />
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{Binding Detail}"
                                           FontSize="11"
                                           Foreground="{StaticResource MutedTextBrush}"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    Margin="12,0,0,0">
                            <Button Content="{Binding PrimaryActionText}"
                                    Click="EditAppButton_Click"
                                    MinWidth="56"
                                    MinHeight="28"
                                    Padding="10,4"
                                    Background="#EEF2F7"
                                    Foreground="#1F2937"
                                    Margin="0,0,6,0" />
                            <Button Content="删除"
                                    Click="RemoveAppButton_Click"
                                    MinWidth="52"
                                    MinHeight="28"
                                    Padding="10,4"
                                    Background="#FEE4E2"
                                    Foreground="#B42318" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>

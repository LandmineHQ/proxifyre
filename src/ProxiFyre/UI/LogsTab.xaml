<UserControl x:Class="ProxiFyre.LogsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <SolidColorBrush x:Key="BorderBrush" Color="#D8DEE8" />

        <Style TargetType="ListBox">
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="0" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="12">
        <ListBox x:Name="LogList"
                 FontFamily="Consolas"
                 FontSize="12"
                 SelectionMode="Extended"
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 PreviewKeyDown="LogList_PreviewKeyDown">
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="复制选中行" Click="CopySelectedMenuItem_Click" />
                    <MenuItem Header="复制全部日志" Click="CopyAllMenuItem_Click" />
                </ContextMenu>
            </ListBox.ContextMenu>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</UserControl>

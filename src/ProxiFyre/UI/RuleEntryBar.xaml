<UserControl x:Class="ProxiFyre.RuleEntryBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#667085" />
        <SolidColorBrush x:Key="BorderBrush" Color="#D8DEE8" />

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="9,5" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource PanelBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="核心进程名"
                           FontSize="12"
                           Foreground="{StaticResource MutedTextBrush}"
                           Margin="0,0,0,4" />
                <TextBox x:Name="CoreProcessNameInput"
                         ToolTip="用于核心进程自排除；默认 steamwebhelper.exe"
                         LostFocus="CoreProcessNameInput_LostFocus"
                         KeyDown="CoreProcessNameInput_KeyDown" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="自定义规则"
                           FontSize="12"
                           Foreground="{StaticResource MutedTextBrush}"
                           Margin="0,0,0,4" />
                <TextBox x:Name="AppInput"
                         ToolTip="例如 chrome.exe、完整 exe 路径，或目录路径" />
            </StackPanel>

            <Button Grid.Column="4"
                    Content="添加"
                    Click="AddButton_Click"
                    VerticalAlignment="Bottom"
                    Background="#2563EB"
                    Foreground="White" />
            <Button Grid.Column="6"
                    Content="应用..."
                    Click="BrowseButton_Click"
                    VerticalAlignment="Bottom"
                    Background="#EEF2F7"
                    Foreground="#1F2937" />
            <Button Grid.Column="8"
                    Content="目录..."
                    Click="BrowseDirectoryButton_Click"
                    VerticalAlignment="Bottom"
                    Background="#EEF2F7"
                    Foreground="#1F2937" />
        </Grid>
    </Border>
</UserControl>
